import { ExpenseCategoryIconPipe } from './expense-category-icon.pipe';

describe('ExpenseCategoryIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpenseCategoryIconPipe();
    expect(pipe).toBeTruthy();
  });
});
