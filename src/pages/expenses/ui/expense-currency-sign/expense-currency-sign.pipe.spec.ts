import { ExpenseCurrencySignPipe } from './expense-currency-sign.pipe';

describe('ExpenseCurrencySignPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpenseCurrencySignPipe();
    expect(pipe).toBeTruthy();
  });
});
